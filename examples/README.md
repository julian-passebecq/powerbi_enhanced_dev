# Examples

Use these to test the transport router without touching production resources.

- local PBIP fixture: create a minimal disposable PBIP project.
- Fabric fixture: use a dedicated non-production workspace.
- never run updateDefinition tests against a real business semantic model.
- DAX Studio bridge can be tested against an open sample PBIX/PBIP.
